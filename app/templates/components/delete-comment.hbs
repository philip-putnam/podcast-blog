<div class="row">
  <div class="col-md-12 well">
    <p>{{comment.author}}, {{comment.date}}</p>
    <p>{{comment.content}}</p>
    <button class="btn btn-danger" {{action 'delete' comment}}>Delete</button>
  </div>
</div>
